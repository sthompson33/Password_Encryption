Password Encryption Program

User can store any number of account information such as username and passwords in encrypted format to a txt file for security.  

Header file named Account
	private: name, password
	public: Account(string, string) setUsername(string u), setPassword(string p), getName(), getPassword()